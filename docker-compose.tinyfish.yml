# TinyFish products as uniquely named TinyBot services (project: tinybot-tinyfish).
#
# This is the host-port overlay for all 15 TinyX products. Product source is not
# vendored, submodule'd, or installed. Each service builds from its private
# GitHub repo (Docker git context, latest default branch) and publishes a unique
# loopback port.
#
# scripts/tinyfish/start-products.ts prefers wrapping each product's own compose
# (sibling checkout or a gitignored .tinyfish-siblings/ clone) as its own Compose
# project. This file is the git-context fallback for every slug. A README-only
# sibling (no Dockerfile yet) is not a TinyBot start failure; cards may show
# Unreachable. TinyPipe remains required locally.
#
# Native ports and compose service names (wrap these, not webhook/postgres):
#   tinypipe     tinyfish-web  3712   platform
#   tinytail     ltdf          8765   platform
#   tinyweb      tinyfish-web  8765   platform
#   tinykit      gallery       8080   platform
#   tinyping     tinyping      8080   board
#   tinytrigger  engine        8080   board (was tf-01-trigger-rules)
#   tinyreg      tinyreg       8080   board
#   tinyscout    tinyscout     8080   board
#   tinybrief    tinybrief     8080   board
#   tinydeed     tinydeed      8080   board
#   tinyfeed     feed          8080   board (was js-02-physical-events)
#   tinyfoundry  tinyfoundry   8080   board
#   tinymargin   tinymargin    8080   board
#   tinyatlas    tinyatlas     8080   board
#   tinyprior    tinyprior     8080   board
# Extra publishes (TinyFeed 8081/8090, TinyTrigger webhook) stay unpublished.
# Each product honors PORT and GET /health → 200 {ok, product, usage_id}.
#
# TinyPipe is the auth socket. Start it first. TinyBot sign-in uses
# TINYFISH_MCP_URL=http://127.0.0.1:3712/mcp and the fixture issuer.

name: tinybot-tinyfish

services:
  tinypipe:
    build:
      context: https://github.com/mariodelgado/tiny-pipe.git
    ports:
      - "127.0.0.1:${TINYPIPE_HOST_PORT:-3712}:${TINYPIPE_PORT:-3712}"
    environment:
      PORT: ${TINYPIPE_PORT:-3712}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinytail:
    build:
      context: https://github.com/mariodelgado/tiny-tail.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYTAIL_HOST_PORT:-18765}:${TINYTAIL_PORT:-8765}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYTAIL_PORT:-8765}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinyweb:
    build:
      context: https://github.com/mariodelgado/tiny-web.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYWEB_HOST_PORT:-18766}:${TINYWEB_PORT:-8765}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYWEB_PORT:-8765}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinykit:
    build:
      context: https://github.com/mariodelgado/tiny-kit.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYKIT_HOST_PORT:-18083}:${TINYKIT_PORT:-8080}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYKIT_PORT:-8080}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinytrigger:
    build:
      context: https://github.com/mariodelgado/tiny-trigger.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYTRIGGER_HOST_PORT:-18081}:${TINYTRIGGER_PORT:-8080}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYTRIGGER_PORT:-8080}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinyfeed:
    build:
      context: https://github.com/mariodelgado/tiny-feed.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYFEED_HOST_PORT:-18082}:${TINYFEED_PORT:-8080}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYFEED_PORT:-8080}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinyping:
    build:
      context: https://github.com/mariodelgado/tiny-ping.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYPING_HOST_PORT:-18101}:${TINYPING_PORT:-8080}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYPING_PORT:-8080}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinyreg:
    build:
      context: https://github.com/mariodelgado/tiny-reg.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYREG_HOST_PORT:-18102}:${TINYREG_PORT:-8080}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYREG_PORT:-8080}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinyscout:
    build:
      context: https://github.com/mariodelgado/tiny-scout.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYSCOUT_HOST_PORT:-18103}:${TINYSCOUT_PORT:-8080}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYSCOUT_PORT:-8080}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinybrief:
    build:
      context: https://github.com/mariodelgado/tiny-brief.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYBRIEF_HOST_PORT:-18104}:${TINYBRIEF_PORT:-8080}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYBRIEF_PORT:-8080}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinydeed:
    build:
      context: https://github.com/mariodelgado/tiny-deed.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYDEED_HOST_PORT:-18105}:${TINYDEED_PORT:-8080}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYDEED_PORT:-8080}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinyfoundry:
    build:
      context: https://github.com/mariodelgado/tiny-foundry.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYFOUNDRY_HOST_PORT:-18106}:${TINYFOUNDRY_PORT:-8080}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYFOUNDRY_PORT:-8080}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinymargin:
    build:
      context: https://github.com/mariodelgado/tiny-margin.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYMARGIN_HOST_PORT:-18107}:${TINYMARGIN_PORT:-8080}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYMARGIN_PORT:-8080}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinyatlas:
    build:
      context: https://github.com/mariodelgado/tiny-atlas.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYATLAS_HOST_PORT:-18108}:${TINYATLAS_PORT:-8080}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYATLAS_PORT:-8080}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}

  tinyprior:
    build:
      context: https://github.com/mariodelgado/tiny-prior.git
    depends_on:
      - tinypipe
    ports:
      - "127.0.0.1:${TINYPRIOR_HOST_PORT:-18109}:${TINYPRIOR_PORT:-8080}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      PORT: ${TINYPRIOR_PORT:-8080}
      TINYFISH_MCP_URL: ${TINYFISH_MCP_URL:-http://host.docker.internal:3712/mcp}
      TINYFISH_ISSUER: ${TINYFISH_ISSUER:-https://issuer.fixtures.tinyfish.test}
